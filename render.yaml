services:
  - type: web
    name: keyword-api-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 app.py        # ← ここを変更！
    pythonVersion: 3.11                 # ← この行も追加！
    plan: free
    region: oregon
