services:
  - type: web
    name: keyword-api-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    pythonVersion: 3.11
    plan: free
    region: oregon
